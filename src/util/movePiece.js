import { removeAllHighlightClasses, pressedElement, findTileByPosition, pressedTile } from "./clickedPiece"

const getDestination = (event) => {

}

const movePiece = (selectedPiece, event, chessBoard) => {
    const validPieceMoves = selectedPiece.getValidMoves



}